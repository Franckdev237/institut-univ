N'oublie pas le rituel pour que ce soit visible sur https://institut-stvalo-obala.web.app :

Enregistre le fichier.

npm run build

firebase deploy



groupe-valerien-education-70bb4 : ID

npm install firebase

// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
import { getAnalytics } from "firebase/analytics";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
// For Firebase JS SDK v7.20.0 and later, measurementId is optional
const firebaseConfig = {
  apiKey: "AIzaSyCR4vBpHc06UC_mVrfxdywNxIlF50xVJ0U",
  authDomain: "groupe-valerien-education.firebaseapp.com",
  projectId: "groupe-valerien-education",
  storageBucket: "groupe-valerien-education.firebasestorage.app",
  messagingSenderId: "1043922077362",
  appId: "1:1043922077362:web:3308129b4e2b09410c6bab",
  measurementId: "G-Y3MP38WGWY"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
const analytics = getAnalytics(app);

j'ai relié mon dossier institut-univ(firebase) à Github pour non seulement permettre la sauvegarde des ficiers mais en plus un rafraichissement rapide du site après chaque modification

voici les étapes dru rafaraichissement du site après modification

- Sur ton PC (D:\institut-univ) : Tu modifies ton code (le texte, les images ou le logo) et tu fais Ctrl + S.

- Vers GitHub : Tu envoies tes modifs avec: un git push.

- Le Robot GitHub (Action) : Dès qu'il reçoit ton code, il se réveille. Il télécharge tes fichiers, installe les dépendances, et lance le npm run build sur ses propres serveurs.

- Mise en ligne : Une fois le build fini, il envoie les fichiers vers https://groupe-valerien-education.web.app/ automatiquement.



git add .
git commit -m "Ajout complet des 10 filières santé et correction build"
git push